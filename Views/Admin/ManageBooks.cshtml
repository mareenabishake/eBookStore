@model IEnumerable<eBookStore.Models.Book>

@{
    ViewData["Title"] = "Manage Books";
}

<h1>Manage Books</h1>

<p>
    <a asp-action="CreateBook" class="btn btn-primary">Create New Book</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>Image</th>
            <th>Title</th>
            <th>Author</th>
            <th>ISBN</th>
            <th>Price</th>
            <th>Stock</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td><img src="@(string.IsNullOrEmpty(item.ImageUrl) ? "/images/default-book.jpg" : item.ImageUrl)" alt="@item.Title" style="width: 50px; height: auto;"></td>
            <td>@item.Title</td>
            <td>@item.Author</td>
            <td>@item.ISBN</td>
            <td>@item.Price.ToString("C")</td>
            <td>@item.StockQuantity</td>
            <td>
                <a asp-action="EditBook" asp-route-id="@item.Id" class="btn btn-sm btn-warning">Edit</a> |
                <a asp-action="DeleteBook" asp-route-id="@item.Id" class="btn btn-sm btn-danger">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
